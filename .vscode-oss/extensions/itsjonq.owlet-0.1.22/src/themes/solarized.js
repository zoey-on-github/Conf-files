const defaultColors = require("../colors/refined");

const config = {
  name: 'Owlet (Solarized)',
  type: 'dark',
}

const shades = {
  background: '#0d2b35',
  text: '#85a5b4',
}

const colors = {
  ...defaultColors,
  blue: '#96beff'
}

module.exports = {
  config,
  colors,
  shades,
}
